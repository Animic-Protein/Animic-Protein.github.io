(()=>{'use strict';const $=s=>document.querySelector(s);let recorder=null,stream=null,chunks=[],startedAt=0,tick=null,blobUrl=null,paused=false;const params=new URLSearchParams(location.search),embedded=params.get('embed')==='1';const start=$('#start'),pause=$('#pause'),stop=$('#stop'),state=$('#state'),timer=$('#timer'),orb=$('#orb'),capture=$('#capture'),audio=$('#audio'),facts=$('#facts'),download=$('#download');if(params.get('context'))$('#context').value=params.get('context');function fmt(ms){const s=Math.floor(ms/1000),m=Math.floor(s/60);return `${String(m).padStart(2,'0')}:${String(s%60).padStart(2,'0')}`;}function setTimer(){timer.textContent=fmt(Date.now()-startedAt);}function cleanupStream(){stream?.getTracks().forEach(t=>t.stop());stream=null;clearInterval(tick);tick=null;orb.classList.remove('live');}function clearCapture(){if(blobUrl)URL.revokeObjectURL(blobUrl);blobUrl=null;audio.removeAttribute('src');audio.load();capture.hidden=true;facts.textContent='';chunks=[];}async function begin(){if(!navigator.mediaDevices?.getUserMedia||!window.MediaRecorder){state.textContent='Aquest navegador no ofereix captura MediaRecorder compatible.';return;}clearCapture();try{stream=await navigator.mediaDevices.getUserMedia({audio:true});const preferred=['audio/webm;codecs=opus','audio/webm','audio/mp4'].find(t=>MediaRecorder.isTypeSupported?.(t));recorder=preferred?new MediaRecorder(stream,{mimeType:preferred}):new MediaRecorder(stream);chunks=[];recorder.ondataavailable=e=>{if(e.data?.size)chunks.push(e.data);};recorder.onstop=finish;recorder.start(250);startedAt=Date.now();tick=setInterval(setTimer,250);setTimer();orb.classList.add('live');state.textContent='Capturant · el contingut encara no és memòria.';start.disabled=true;pause.disabled=false;stop.disabled=false;paused=false;pause.textContent='Pausa';window.dispatchEvent(new CustomEvent('codex:recordare-started',{detail:{capturedAt:new Date().toISOString(),sourceKind:'media',mediaKind:'audio'}}));}catch(err){cleanupStream();state.textContent=err?.name==='NotAllowedError'?'Micròfon no autoritzat. RECORDARE no captura sense permís.':'No s’ha pogut iniciar la captura.';}}function togglePause(){if(!recorder)return;if(recorder.state==='recording'){recorder.pause();paused=true;pause.textContent='Reprendre';state.textContent='Captura en pausa.';}else if(recorder.state==='paused'){recorder.resume();paused=false;pause.textContent='Pausa';state.textContent='Capturant.';}}function end(){if(recorder&&(recorder.state==='recording'||recorder.state==='paused'))recorder.stop();}function finish(){const duration=Date.now()-startedAt,mime=recorder?.mimeType||chunks[0]?.type||'audio/webm',blob=new Blob(chunks,{type:mime});cleanupStream();blobUrl=URL.createObjectURL(blob);audio.src=blobUrl;capture.hidden=false;const name=$('#name').value.trim()||`RECORDARE ${new Date().toLocaleString('ca-ES')}`;download.href=blobUrl;download.download=name.replace(/[\\/:*?"<>|]+/g,'-')+(mime.includes('mp4')?'.m4a':'.webm');facts.textContent=`índex: ${name} · ${fmt(duration)} · ${mime} · ${Math.max(1,Math.round(blob.size/1024))} KB · source.kind=media · localOnly`;state.textContent='Captura finalitzada · encara no conservada ni canonitzada.';start.disabled=false;pause.disabled=true;stop.disabled=true;timer.textContent=fmt(duration);const detail={id:`recordare-${Date.now().toString(36)}`,name,duration,mimeType:mime,size:blob.size,source:{kind:'media',mediaKind:'audio'},context:$('#context').value.trim(),node:params.get('node')||location.pathname,invokedBy:params.get('invokedBy')||'RECORDARE · Instrument',consent:$('#consent').checked,storageRef:null,localOnly:true,canonical:false,provenance:{kind:'recordare-capture',capturedAt:new Date().toISOString(),reversible:true}};window.dispatchEvent(new CustomEvent('codex:recordare-captured',{detail}));if(embedded&&window.parent!==window)window.parent.postMessage({type:'codex:recordare-captured',detail},location.origin);}function discard(){clearCapture();timer.textContent='00:00';state.textContent='Captura descartada. Cap contingut conservat.';window.dispatchEvent(new CustomEvent('codex:recordare-discarded',{detail:{reversible:true}}));}start.addEventListener('click',begin);pause.addEventListener('click',togglePause);stop.addEventListener('click',end);$('#discard').addEventListener('click',discard);window.addEventListener('pagehide',()=>{cleanupStream();if(blobUrl)URL.revokeObjectURL(blobUrl);});window.Recordare={start:begin,stop:end,discard};})();